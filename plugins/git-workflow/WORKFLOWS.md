# Workflows

Visual reference for all git-workflow commands.

## Quick Reference

| Command | Steps | Automation |
|---------|-------|------------|
| `/git-commit` | 2 | Full |
| `/git-commit-push` | 3 | Full |
| `/git-commit-push-pr` | 4 | Full |
| `/git-bisect` | 6 | Semi (confirms script) |
| `/git-pr-review` | 11 | Interactive |
| `/git-help-rebase` | 4 | Interactive |
| `/git-why` | 4 | Interactive |
| `/git-changelog` | 3 | Semi (confirms output) |
| `/git-clean-branches` | 3 | Full |

---

## /git-commit

Create a commit with Gitmoji conventions.

```
┌─────────────────────────────────────┐
│           /git-commit               │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 1. STAGE                            │
│    └─ git add (if nothing staged)   │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 2. COMMIT                           │
│    ├─ Gitmoji + message             │
│    ├─ Detailed description          │
│    └─ Refs: TICKET-123 (if branch)  │
└─────────────────────────────────────┘
```

---

## /git-commit-push

Commit and push in one command.

```
┌─────────────────────────────────────┐
│        /git-commit-push             │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 1. STAGE                            │
│    └─ git add (if nothing staged)   │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 2. COMMIT                           │
│    └─ Gitmoji conventions           │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 3. PUSH                             │
│    └─ git push origin <branch>      │
└─────────────────────────────────────┘
```

---

## /git-commit-push-pr

Full workflow: commit, push, and create PR.

```
┌─────────────────────────────────────┐
│      /git-commit-push-pr            │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 1. DETECT PARENT BRANCH             │
│    ├─ On main/master/develop?       │
│    │   └─ Create feature branch     │
│    └─ On feature branch?            │
│        └─ Find parent via merge-base│
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 2. COMMIT                           │
│    └─ Gitmoji conventions           │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 3. PUSH                             │
│    └─ git push -u origin <branch>   │
└─────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 4. CREATE PR                        │
│    ├─ GitHub: gh pr create          │
│    ├─ GitLab: glab mr create        │
│    └─ Bitbucket: curl API           │
│                                     │
│    Target: parent branch            │
└─────────────────────────────────────┘
```

---

## /git-bisect

Find the commit that introduced a bug.

```
┌─────────────────────────────────────────────────────────────┐
│                      /git-bisect                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. GATHER INFO                                              │
│    ├─ What's the bug?                                       │
│    ├─ Bad commit (default: HEAD)                            │
│    └─ Good commit (tag or older commit)                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. CREATE TEST SCRIPT                                       │
│    └─ ./bisect-test.sh                                      │
│       ├─ exit 0 → bug NOT present (good)                    │
│       ├─ exit 1 → bug IS present (bad)                      │
│       └─ exit 125 → can't test (skip)                       │
│                                                             │
│    User confirms script before continuing                   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. RUN BISECT                                               │
│    └─ git bisect start <bad> <good>                         │
│       git bisect run ./bisect-test.sh                       │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. SHOW RESULT                                              │
│    🎯 Culprit Found                                         │
│    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                 │
│    📍 Commit + 📝 Message + 👤 Author + 📅 Date             │
│    📁 Files changed                                         │
│    🔬 Analysis (what likely caused the bug)                 │
│    🔗 Link to commit                                        │
│    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. PROPOSE FIX                                              │
│    "Do you want me to fix this bug and create a PR?"        │
└─────────────────────────────────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              ▼                           ▼
┌──────────────────────────┐    ┌─────────────────────────────┐
│ YES                      │    │ NO                          │
│ ├─ git bisect reset      │    │ └─ git bisect reset         │
│ ├─ rm ./bisect-test.sh   │    │    rm ./bisect-test.sh      │
│ ├─ git checkout -b fix/  │    └─────────────────────────────┘
│ ├─ Implement fix         │
│ ├─ Verify with test      │
│ └─ /git-commit-push-pr   │
└──────────────────────────┘
```

---

## /git-pr-review

Review a PR with AI-assisted analysis.

```
┌─────────────────────────────────────────────────────────────┐
│                    /git-pr-review                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. SAVE CONTEXT                                             │
│    ├─ Store current branch                                  │
│    └─ git stash (if uncommitted changes)                    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. DETECT PROVIDER                                          │
│    ├─ github.com → GitHub                                   │
│    ├─ gitlab.com → GitLab                                   │
│    └─ bitbucket.org → Bitbucket                             │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. LIST & SELECT PR                                         │
│    └─ Show open PRs, user picks one                         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. CHECKOUT PR                                              │
│    └─ gh pr checkout / glab mr checkout / git fetch         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. GET CHANGED FILES                                        │
│    └─ git diff origin/$BASE...HEAD --name-only              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. REVIEW EACH FILE (loop)                                  │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ 📄 [X/Y] file.ts:42 (+N/-M lines)                   │  │
│    │ 📝 Changes: <diff>                                  │  │
│    │ 🔍 Analysis: <explanation>                          │  │
│    │ 💬 Suggested comments:                              │  │
│    │    1. L:47 [security] "Validate input..."           │  │
│    │    2. L:52 [perf] "Consider caching..."             │  │
│    │                                                     │  │
│    │ Actions: "1" post, "next" skip, "done" finish       │  │
│    └─────────────────────────────────────────────────────┘  │
│                                                             │
│    Wait for user input → post comments → next file          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. FINALIZE                                                 │
│    ├─ approve → Approve PR                                  │
│    ├─ request-changes → Request changes                     │
│    ├─ comment → Comment only                                │
│    └─ skip → No action                                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. RESTORE CONTEXT                                          │
│    ├─ git checkout <original-branch>                        │
│    └─ git stash pop (if stashed)                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 9. SUMMARY                                                  │
│    ├─ PR status (approved/changes/commented)                │
│    ├─ Files reviewed: X/Y                                   │
│    ├─ Comments posted: N                                    │
│    └─ 🔗 PR link                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## /git-help-rebase

Interactive assistant to resolve rebase conflicts.

```
┌─────────────────────────────────────────────────────────────┐
│                   /git-help-rebase                          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. ANALYZE CURRENT STATE                                    │
│    ├─ git status                                            │
│    ├─ Identify conflicted files                             │
│    └─ Understand rebase progress                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. FOR EACH CONFLICT (loop)                                 │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ Show both versions:                                 │  │
│    │ ├─ OURS (current branch)                            │  │
│    │ └─ THEIRS (incoming changes)                        │  │
│    │                                                     │  │
│    │ Explain what each version does                      │  │
│    │ Suggest resolution                                  │  │
│    └─────────────────────────────────────────────────────┘  │
│                                                             │
│    User chooses → Apply resolution → git add                │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. CONTINUE REBASE                                          │
│    └─ git rebase --continue                                 │
│                                                             │
│    If more conflicts → back to step 2                       │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. COMPLETE                                                 │
│    └─ Rebase finished successfully                          │
└─────────────────────────────────────────────────────────────┘
```

---

## /git-why

Investigate git history to explain why code exists.

```
┌─────────────────────────────────────────────────────────────┐
│                       /git-why                              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. UNDERSTAND QUESTION                                      │
│    ├─ What to understand? (complexity, workaround, bug)     │
│    └─ Which file/function/lines?                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. INVESTIGATE                                              │
│    ├─ git blame <file>                                      │
│    ├─ git log -p --follow -- <file>                         │
│    ├─ git log -S "<code>" (pickaxe)                         │
│    ├─ git log -L <start>,<end>:<file>                       │
│    └─ git show <commit>                                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. REASON & CONNECT                                         │
│    ├─ Find patterns in evolution                            │
│    ├─ Identify root causes                                  │
│    ├─ Understand intent from messages                       │
│    └─ Connect related changes                               │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. TELL THE STORY                                           │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ "This regex evolved over 8 months:"                 │  │
│    │                                                     │  │
│    │ 1. a1b2c3 (Alice, March) - Initial \d+ pattern      │  │
│    │ 2. d4e5f6 (Bob, May) - Added UUID support           │  │
│    │ 3. g7h8i9 (Alice, July) - Bug #234 fix              │  │
│    │ 4. j0k1l2 (Charlie, Oct) - Production incident      │  │
│    │                                                     │  │
│    │ Complexity = backwards compatibility with 3 formats │  │
│    └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## /git-changelog

Generate a changelog from commits.

```
┌─────────────────────────────────────────────────────────────┐
│                    /git-changelog                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. DETERMINE RANGE                                          │
│    ├─ From arguments: v1.0.0..v1.1.0                        │
│    └─ Or use last two tags                                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. GET & GROUP COMMITS                                      │
│    ├─ git log <range> --pretty=format:"%h|%s|%an"           │
│    │                                                        │
│    │ Group by Gitmoji:                                      │
│    │ ├─ ✨ Features                                         │
│    │ ├─ 🐛 Bug Fixes                                        │
│    │ ├─ 📝 Documentation                                    │
│    │ ├─ ♻️ Refactoring                                      │
│    │ └─ ...                                                 │
│    │                                                        │
│    └─ Extract contributors                                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. OUTPUT                                                   │
│    ├─ Print to console (default)                            │
│    ├─ Prepend to CHANGELOG.md                               │
│    └─ Copy to clipboard                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## /git-clean-branches

Clean up stale local branches.

```
┌─────────────────────────────────────────────────────────────┐
│                  /git-clean-branches                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. LIST BRANCHES                                            │
│    └─ git branch -v                                         │
│       Identify branches marked [gone]                       │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. CHECK WORKTREES                                          │
│    └─ git worktree list                                     │
│       Identify associated worktrees                         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. CLEANUP (for each [gone] branch)                         │
│    ├─ Remove worktree if exists                             │
│    └─ git branch -D <branch>                                │
└─────────────────────────────────────────────────────────────┘
```
